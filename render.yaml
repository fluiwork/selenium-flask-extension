services:
  - type: web
    name: selenium-flask
    env: python
    buildCommand: |
      apt-get update && apt-get install -y xvfb
      pip install -r requirements.txt
    startCommand: xvfb-run -a python3 selenium_flask_app.py
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
